# 安全策略

[English](SECURITY.en.md) | 简体中文

## 支持范围

安全修复只面向最新正式版本和 `main` 分支。旧版本可能需要先升级后再确认问题。

## 私密报告漏洞

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。不要在公开 Issue 中提交利用代码、代理凭据、令牌、私钥、真实内网地址或包含个人信息的日志。

报告应包含：受影响版本与平台、复现步骤、影响范围、最小化日志，以及你建议的缓解方式。若仓库尚未启用私密漏洞报告，请只创建不含敏感细节的公开 Issue，请维护者提供私密联系渠道。

## 重要边界

- Haruha 会修改当前用户的系统代理设置。
- PAC 服务只绑定 `127.0.0.1`，不应暴露到局域网。
- 配置和日志保存在本机用户配置目录，不应提交到仓库。
- IP 查询、测速和图标获取会访问第三方服务；不要把敏感 URL 放入测速配置。
- 强制退出可能保留系统代理状态；网络异常时可在操作系统设置中手动关闭。

安全边界的技术细节见[架构说明](docs/zh-CN/architecture.md#安全模型)。
